var nodeConsoleFactory = require('../index');
var nodeConsole = nodeConsoleFactory(require('../test/consolefile.js'));

describe('Constructor', function () {

    it('should throw with bad credentials', function () {
    });

  });
